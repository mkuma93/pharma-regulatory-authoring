# === HTTP client (service-to-service calls) ===
httpx>=0.27.0

# === API ===
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# === Config / validation ===
pydantic>=2.0.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
