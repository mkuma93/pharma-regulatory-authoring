# === HTTP client (service-to-service calls) ===
httpx>=0.27.0

# === API ===
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# === Config / validation ===
pydantic>=2.0.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# === GCP auth (OIDC tokens for Cloud Run service-to-service calls) ===
google-auth>=2.29.0
google-auth-httplib2
requests>=2.32.0
